{{#each words as |word|}}
  {{log word.word}}
  <section data-id="{{word.id}}">
    <h4>
      {{word.word}}
      {{word.id}}
    </h4>
    <br>
    <form id="update-delete">
      <fieldset>
        <a href="#" class="update-link" data-id="{{word.id}}" data-toggle="modal" data-target="#update-modal">edit</a>
        &nbsp; • &nbsp;
        <a href="#" class="delete-button" data-id="{{word.id}}">delete</a>
      </fieldset>
    </form>
    <hr>

  <!-- <div class="modal" id="update-modal" tabindex="-1" role="dialog">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">edit word</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="update-modal">
        <form class="update-word-form">
          <fieldset>
            <input type="text" name="word[word]" placeholder="new word" autocomplete="on">
            <br>
            <input class="btn btn-outline-info update-word-form update-button" type="submit" value="reset">
          </fieldset>
        </form>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
      </div>
    </div>
  </div>
</div> -->
</section>

{{/each}}
